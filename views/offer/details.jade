extends ../template/layout
include ../mixins/form-helper

block content
  .row
    .small-12.columns
      h3 Confirmed
      each offer in buyer.offers
        if offer.isConfirmed
          p= offer.artist
  .row
    .small-12.columns
      h3 Pending
      each offer in buyer.offers
        if !offer.isConfirmed
          p= offer.artist

      //- p= offer.artist
      //- p= offer.showDate
      //- p#street= offer.address
      //- p#city= offer.city
      //- p#state=offer.state
      //- p#zip=offer.zip
  .row
    .small-12.columns
      #map-canvas